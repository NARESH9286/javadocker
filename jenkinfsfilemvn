pipeline {
    agent any

    stages {
        stage('install this maven software') {
            steps {
               sh 'sudo dnf install maven -y'
            }
        }
     stage('creation of package') {
            steps {
             sh 'mvn clean package'
            }
        }
     stage('releasing pacakge') {
            steps {
             archiveArtifacts artifacts: 'target/*.jar', followSymlinks: false
            }
        }
    }
}
